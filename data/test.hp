func inc(param n)
{
	m := n;
	m = m + 1;

    return m;
};

func main()
{
	c := 2;
	a := 2 * c;
	n := -a;

	while (n - 10) {
		n := --inc(n);
	};

	return 1;
};

func decr(param n)
{
	m := n;
	m = m + 1;

	return m;
};